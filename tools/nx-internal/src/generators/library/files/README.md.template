# <%= name %>

## Lint

Run `nx lint <%= name %>` to lint the library.

## Running unit tests

Run `nx test <%= name %>` to execute the unit tests via [Jest](https://jestjs.io).

## Build

Run `nx build <%= name %>` to build this project

## Publish

Run `nx publish <%= name %>` to dry run publish this project.

** Note **: Actual publishing is handled by CI/CD. We do not accept local publishing
